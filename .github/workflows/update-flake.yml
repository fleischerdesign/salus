name: Update Flake Lock

on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  update-flake:
    uses: fleischerdesign/workflows/.github/workflows/update-flake.yml@v1
    secrets: inherit
